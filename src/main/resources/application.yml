server:
  port: 7000

spring:
  application:
    name: member-service

# Eureka
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://127.0.0.1:8088/eureka/}
    register-with-eureka: true
    fetch-registry: true